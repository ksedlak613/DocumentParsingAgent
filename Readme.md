# Document & Audio LLM Parser

A lightweight parser that extracts insights from **documents and audio files** using LLMs.

## Features
- 📄 Text / document parsing
- 🎙️ Audio transcription + analysis
- 🧠 LLM-powered structured output
- Minimal, clean, extensible

## Setup
```bash
git clone https://github.com/ksedlak613/DocumentParsingAgent
cd llm-parser
pip install -r requirements.txt
export OPENAI_API_KEY=your_key
